#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 * Description: print leters of the alphabet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	char om = 'a';

	while (om <= 'z')
	{
		putchar(om);
		c++;
	}
	putchar('\n');
	return (0);
}
